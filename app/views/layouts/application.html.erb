<!DOCTYPE html>
<html lang="de">
<head>
  <title>Test1</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
	<p class="notice"><%= notice %></p>
	<p class="alert"><%= alert %></p>

	<% if user_signed_in? %>
		Angemeldet als <%= current_user.email %>. Falsch? <%= link_to "Abmelden", destroy_user_session_path %>
	<% else %>
		<%= link_to "Anmelden", new_user_registration_path %>
	<% end %>

	<%= yield %>
</body>
</html>
